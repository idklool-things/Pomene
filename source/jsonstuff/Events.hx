package jsonstuff;

/**
 * Eventos
 * Autoexplicativo, huh?
 * Não vou fazer editor para isso, tá me dando (MAIS) ansiedade.
 * Ninguém vai usar isso mesmo, então eh.
 */

typedef EventVars = {
  var time:Float;
  var name:String;
  var val1:String;
  var val2:String;
}
/**
 * Isso ia ser usado, mas por motivos de bugs (novidade?),
 * vai ficar de enfeite.
 */
class Events {
  public function new() {}
}